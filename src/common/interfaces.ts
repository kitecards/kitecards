export type User = {
  id: string
  name: string
  email: string
  imageUrl: string
}

export type Promo = {
  id: string
  storeId: string
  storeName: string
  storeLogo: string
  tagLine: string
  description: string
  location: string
}
